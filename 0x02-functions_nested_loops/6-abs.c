#include "main.h"

/**
 * _abs - Computs the absolute value of an integer.
 * @n: Integer to be compiled
 * Return: The absolute value of the integer.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
